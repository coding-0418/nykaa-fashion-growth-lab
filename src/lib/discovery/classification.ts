/**
 * Classification of journey/intent/outcome is performed during extraction parsing.
 * Clustering of new themes is intentionally not implemented in this milestone.
 */

export function isClassificationImplemented(): boolean {
  return true;
}
